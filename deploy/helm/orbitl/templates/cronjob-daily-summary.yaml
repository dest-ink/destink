{{- if .Values.cronJobs.dailySummary.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "orbitl.fullname" . }}-daily-summary
  labels:
    {{- include "orbitl.labels" . | nindent 4 }}
    app.kubernetes.io/component: daily-summary
spec:
  schedule: {{ .Values.cronJobs.dailySummary.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "orbitl.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: daily-summary
        spec:
          restartPolicy: OnFailure
          containers:
            - name: daily-summary
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.jobs.image.tag }}"
              imagePullPolicy: IfNotPresent
              command: ["npx", "tsx", "src/jobs/daily-summary.ts"]
              envFrom:
                - secretRef:
                    name: {{ include "orbitl.fullname" . }}
              resources:
                {{- toYaml .Values.jobs.resources | nindent 16 }}
{{- end }}
