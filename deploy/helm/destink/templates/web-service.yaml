apiVersion: v1
kind: Service
metadata:
  name: {{ include "destink.fullname" . }}-web
  labels:
    {{- include "destink.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.web.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "destink.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
