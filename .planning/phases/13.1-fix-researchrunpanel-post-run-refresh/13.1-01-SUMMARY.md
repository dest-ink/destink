---
phase: 13.1-fix-researchrunpanel-post-run-refresh
plan: 01
subsystem: ui
tags: [react, next.js, router-refresh, sse, research]

# Dependency graph
requires:
  - phase: 13-draft-generation
    provides: GenerateDraftsButton with router.refresh() pattern
provides:
  - ResearchRunPanel with post-run router.refresh() that updates server components after SSE stream closes
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "router.refresh() in finally block: call after setRunning(false) to re-fetch server components and show updated data without manual page reload"

key-files:
  created: []
  modified:
    - src/components/research/ResearchRunPanel.tsx

key-decisions:
  - "router.refresh() called unconditionally in finally block (not conditionally) -- ensures server state syncs even if run fails, matching GenerateDraftsButton pattern"

patterns-established:
  - "SSE stream finally block: setRunning(false) then router.refresh() -- same order as GenerateDraftsButton.tsx"

requirements-completed: [DRAFT-02]

# Metrics
duration: 1min
completed: 2026-03-14
---

# Phase 13.1 Plan 01: Fix ResearchRunPanel Post-Run Refresh Summary

**router.refresh() added to ResearchRunPanel finally block so runs list page updates automatically after SSE stream closes**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-03-14T19:42:49Z
- **Completed:** 2026-03-14T19:43:24Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `useRouter` import from `next/navigation` to ResearchRunPanel
- Added `const router = useRouter()` hook call in component body
- Added `router.refresh()` to the `handleRun` finally block after `setRunning(false)`
- Mirrors the exact pattern used in GenerateDraftsButton.tsx (lines 3, 30, 115)
- TypeScript compiles without errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Add router.refresh() to ResearchRunPanel finally block** - `3f372c8` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/components/research/ResearchRunPanel.tsx` - Added useRouter import, hook call, and router.refresh() in finally block

## Decisions Made
- router.refresh() placed unconditionally in finally block (not conditional on success) -- this matches GenerateDraftsButton.tsx and ensures server state is always refreshed, even on error (stale run list is worse than a no-op refresh)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Integration gap DRAFT-02 closed: runs list page will now show new run rows and draft badges immediately after ResearchRunPanel completes
- No blockers for Phase 14 (Automation Config)

---
*Phase: 13.1-fix-researchrunpanel-post-run-refresh*
*Completed: 2026-03-14*

## Self-Check: PASSED

- FOUND: src/components/research/ResearchRunPanel.tsx
- FOUND: commit 3f372c8
