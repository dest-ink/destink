---
phase: 13.1-fix-researchrunpanel-post-run-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/research/ResearchRunPanel.tsx
autonomous: true
requirements:
  - DRAFT-02
gap_closure: true

must_haves:
  truths:
    - "After a research run completes via ResearchRunPanel, the runs list page updates to show the new run without a manual page reload"
    - "If auto-draft generated drafts, the draft count badge appears on the new run row immediately after run completes"
  artifacts:
    - path: "src/components/research/ResearchRunPanel.tsx"
      provides: "Research run panel with post-run router refresh"
      contains: "router.refresh()"
  key_links:
    - from: "src/components/research/ResearchRunPanel.tsx"
      to: "next/navigation useRouter"
      via: "router.refresh() in finally block"
      pattern: "router\\.refresh\\(\\)"
---

<objective>
Add router.refresh() to ResearchRunPanel so the runs list page updates after a research run completes, closing the integration gap identified in the v1.2 milestone audit.

Purpose: ResearchRunPanel currently does not refresh server components after its SSE stream closes. The runs list page shows stale data until the user manually reloads. GenerateDraftsButton already implements this pattern correctly -- mirror it.

Output: Updated ResearchRunPanel.tsx with useRouter import and router.refresh() call in the finally block.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/research/ResearchRunPanel.tsx
@src/components/research/GenerateDraftsButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add router.refresh() to ResearchRunPanel finally block</name>
  <files>src/components/research/ResearchRunPanel.tsx</files>
  <action>
    Mirror the pattern from GenerateDraftsButton.tsx (lines 3, 30, 115):

    1. Add `import { useRouter } from 'next/navigation';` alongside existing imports
    2. Inside the `ResearchRunPanel` component function, add `const router = useRouter();` before the existing useState hooks
    3. In the `handleRun` function's `finally` block (currently line 89-91), add `router.refresh();` after `setRunning(false);`

    The finally block should become:
    ```
    finally {
      setRunning(false);
      router.refresh();
    }
    ```

    This is the exact same pattern used in GenerateDraftsButton.tsx. The router.refresh() call tells Next.js to re-fetch server components on the current route, which causes the runs list (a server component) to re-query the database and show the newly created run row and any draft badges.

    Do NOT add any other changes -- this is a surgical one-line fix plus one import and one hook call.
  </action>
  <verify>
    <automated>cd /Users/dknell/Projects/orbitl && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    ResearchRunPanel.tsx imports useRouter from next/navigation, calls useRouter() in the component body, and calls router.refresh() in the handleRun finally block. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors related to ResearchRunPanel
2. `grep -n "router.refresh" src/components/research/ResearchRunPanel.tsx` shows the refresh call
3. `grep -n "useRouter" src/components/research/ResearchRunPanel.tsx` shows the import and hook usage
</verification>

<success_criteria>
- ResearchRunPanel.tsx calls router.refresh() in its finally block after the SSE stream closes
- TypeScript compiles without errors
- The pattern matches GenerateDraftsButton.tsx (useRouter import, hook call, refresh in finally)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-fix-researchrunpanel-post-run-refresh/13.1-01-SUMMARY.md`
</output>
