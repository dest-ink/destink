---
phase: 13.1-fix-researchrunpanel-post-run-refresh
verified: 2026-03-14T20:00:00Z
status: passed
score: 2/2 must-haves verified
re_verification: false
---

# Phase 13.1: Fix ResearchRunPanel Post-Run Refresh Verification Report

**Phase Goal:** After a research run completes, the runs list page refreshes automatically so the new run row and draft badge appear without a manual page reload
**Verified:** 2026-03-14T20:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                                              | Status     | Evidence                                                                                                         |
| -- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------- |
| 1  | After a research run completes via ResearchRunPanel, the runs list page updates to show the new run without a manual page reload   | VERIFIED   | `router.refresh()` called unconditionally in `handleRun` finally block (line 93), after `setRunning(false)`     |
| 2  | If auto-draft generated drafts, the draft count badge appears on the new run row immediately after run completes                  | VERIFIED   | `router.refresh()` triggers server component re-render; RunsPage re-queries DB including `draftsGenerated` field |

**Score:** 2/2 truths verified

### Required Artifacts

| Artifact                                              | Expected                                        | Status     | Details                                                                                          |
| ----------------------------------------------------- | ----------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------ |
| `src/components/research/ResearchRunPanel.tsx`        | Research run panel with post-run router refresh | VERIFIED   | 185 lines, substantive SSE stream reader, `useRouter` import on line 3, hook on line 22, `router.refresh()` on line 93 |

### Key Link Verification

| From                               | To                         | Via                                   | Status  | Details                                                                                                             |
| ---------------------------------- | -------------------------- | ------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------- |
| `ResearchRunPanel.tsx`             | `next/navigation useRouter` | `router.refresh()` in finally block   | WIRED   | `import { useRouter } from 'next/navigation'` (line 3), `const router = useRouter()` (line 22), call at line 93    |
| `ResearchRunPanel.tsx`             | `runs/page.tsx`            | imported and rendered                 | WIRED   | Imported at line 7 of `src/app/(app)/research/[id]/runs/page.tsx`, rendered at line 71                             |
| `runs/page.tsx` (server component) | Drizzle DB query           | re-executed on `router.refresh()`     | WIRED   | Page marked `export const dynamic = 'force-dynamic'` (line 10); re-query includes `draftsGenerated` field (line 35)|

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                       | Status    | Evidence                                                                                                                    |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------- |
| DRAFT-02    | 13.1-01     | Research engine auto-generates drafts after a run completes, respecting maxDraftsPerRun setting   | SATISFIED | `router.refresh()` in ResearchRunPanel closes the UI feedback gap: runs list re-fetches from DB including drafts badge data |

**Note on DRAFT-02 scope:** DRAFT-02 was originally completed in Phase 13 (server-side auto-draft engine). Phase 13.1 closes the remaining UI feedback gap — the server already wrote drafts, but the client didn't refresh to show them. The requirement is fully satisfied end-to-end.

### Anti-Patterns Found

None. No TODOs, FIXMEs, placeholder returns, or stub implementations found in `ResearchRunPanel.tsx`.

### Human Verification Required

#### 1. End-to-end refresh behavior

**Test:** Navigate to a researcher's runs page, click "Run Research", wait for the SSE stream to complete.
**Expected:** The "Past Runs" list below the panel updates in-place to show the new run row (with source count, topic count, and draft badge if auto-draft ran) without a manual page reload.
**Why human:** `router.refresh()` triggers a Next.js server component re-fetch at runtime; the correctness of the refresh and the visual appearance of the new row can only be confirmed in a live browser session.

#### 2. Draft badge visibility after auto-draft run

**Test:** Run research with a researcher configured for `autoDraft: true` and `maxDraftsPerRun > 0`. Wait for run plus draft generation to complete.
**Expected:** The draft badge (showing draft count) appears on the new run row in the "Past Runs" list immediately after the panel finishes, without a page reload.
**Why human:** Requires a full E2E flow (researcher config + live LLM draft generation); cannot verify badge rendering programmatically.

### Gaps Summary

No gaps. All automated checks passed:
- `router.refresh()` is present at line 93 in the `finally` block, called after `setRunning(false)`, mirroring the GenerateDraftsButton.tsx pattern exactly.
- `useRouter` is imported from `next/navigation` (line 3) and instantiated (line 22).
- `ResearchRunPanel` is wired into the runs page (`runs/page.tsx`) which is `force-dynamic`, ensuring each `router.refresh()` triggers a real DB re-fetch.
- Commit `3f372c8` exists and modifies exactly the one file claimed.
- TypeScript compiles without errors.
- DRAFT-02 is the sole requirement ID declared in the plan; it maps correctly to this fix.

---

_Verified: 2026-03-14T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
